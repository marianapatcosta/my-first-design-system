export enum KEYBOARD_KEY {
  ENTER_KEY = 'Enter',
  ESCAPE_KEY = 'Escape',
  SPACE_KEY = ' ',
  LEFT_ARROW_KEY = 'ArrowLeft',
  UP_ARROW_KEY = 'ArrowUp',
  RIGHT_ARROW_KEY = 'ArrowRight',
  DOWN_ARROW_KEY = 'ArrowDown',
}

export enum TOAST_TYPE {
  ALERT = 'alert',
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
}

export enum ORIENTATION {
  LANDSCAPE = 'landscape',
  VERTICAL = 'vertical',
}

export enum DOCUMENT_ORIENTATION {
  LANDSCAPE = 'landscape',
  PORTRAIT = 'portrait',
  L = 'l',
  P = 'p',
}

export enum FILE_TYPE {
  CSV = '.csv',
  JPEG = '.jpeg',
  PNG = '.png',
  SVG = '.svg',
  PDF = '.pdf',
  XLS = '.xls',
  XLSX = '.xlsx',
}

export enum PREVIEWS_POSITION {
  BEFORE = 'before',
  AFTER = 'after',
}
