import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Input } from "./Input";
import { Success } from '../../assets/icons';

<Meta title="Design System/Input" component={Input} />

# Input
<br/><br/> 

### Displays a customized Input field, to insert small amount of data

Use input field when it is supposed that the user provides some written information

<Preview withToolbar>
  <Story name="Standard">
    <Input value="hello" />
  </Story>
</Preview>

<ArgsTable of={Input} />

### Input with Label:

  <Story name="WithLabel">
    <Input value="hello" label="Input" />
  </Story>

### Input with Icon:

  <Story name="WithIcon">
    <Input value="hello" icon={Success} />
  </Story>

### Input with Error:
  <Story name="WithError">
    <Input value="hello" errorMessage="This is an error!" />
  </Story>


### Playground

Experiment with Input Component with the Controls addon in the Canvas tab.

export const Template = (args) => <Input {...args} />

<Canvas>
  <Story name="Controls" args={{
    value: 'hello'
  }}>
    {Template.bind({})}
  </Story>
</Canvas> 
