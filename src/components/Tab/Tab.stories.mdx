import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Tab } from "./Tab";

<Meta title="Design System/Tab" component={Tab} />

# Tab
<br/><br/> 

### Displays a content integrated in a Tabs component.

Use Tab as child of 'Tabs' component; each Tab's content will only be visible when the tab is active

<Preview withToolbar>
  <Story name="Standard">
    <ul style={{ listStyle: 'none' }} role='tablist'>
      <Tab label="Tab 1" />
    </ul>)
  </Story>
</Preview>

<ArgsTable of={Tab} />

### Active Tab:
  <Story name="Active">
   <ul style={{ listStyle: 'none' }} role='tablist'>
    <Tab label="Tab 1" isActive={true} />
  </ul>)
  </Story>

### Playground

Experiment with Tab Component with the Controls addon in the Canvas tab.

export const Template = (args) => (
  <ul style={{ listStyle: 'none' }} role='tablist'>
    <Tab {...args} />
  </ul>)

<Canvas>
  <Story name="Controls" args={{  
    label: 'Tab 1' 
  }}>
    {Template.bind({})}
  </Story>
</Canvas> 