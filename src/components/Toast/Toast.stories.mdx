import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Toast } from "./Toast";

<Meta title="Design System/Toast" component={Toast} argTypes={{
    type: {
      control: {
        type: 'radio',
        options: ['info', 'alert', 'warning', 'success']
      }
    }
  }}/>

# Toast
<br/><br/> 

### Displays a temporary message on the screen

Use Toast to display a temporary message for the user. There are 4 toast types available:
_alert_, _info_, _success_ and _warning_

<Preview withToolbar>
  <Story name="Standard">
    <Toast message="info toast" style={{position: 'relative', top: 'revert'}}/>
  </Story>
</Preview>

<ArgsTable of={Toast} />

### Alert Toast:

  <Story name="Alert">
    <Toast message="alert toast" type="alert" style={{position: 'relative', top: 'revert', marginLeft: '1rem'}}/>
  </Story>

### Warning Toast:

  <Story name="Warning">
    <Toast message="warning toast" type="warning" style={{position: 'relative', top: 'revert', marginLeft: '1rem'}}/>
  </Story>

### Success Toast:
  <Story name="Success">
    <Toast message="success toast" type="success" style={{position: 'relative', top: 'revert', marginLeft: '1rem'}} />
  </Story>

### Playground

Experiment with Toast Component with the Controls addon in the Canvas tab.

export const Template = (args) => <Toast {...args} />

<Canvas>
  <Story name="Controls" args={{
   message: 'toast message',
   style: {position: 'relative', top: 'revert', marginLeft: '1rem'}
  }}>
    {Template.bind({})}
  </Story>
</Canvas> 