import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { ToggleSwitch } from "./ToggleSwitch";

<Meta title="Design System/ToggleSwitch" component={ToggleSwitch} />

# ToggleSwitch
<br/><br/> 

### Allows to alternate between two values

Use ToggleSwitch when the value alternates between two values; mimics checkbox behavior but has different styling

<Preview withToolbar>
  <Story name="Standard">
    <ToggleSwitch checked={false} leftLabel="off" rightLabel="on" label="ToggleSwitch"/>
  </Story>
</Preview>

<ArgsTable of={ToggleSwitch} />

### Playground

Experiment with ToggleSwitch Component with the Controls addon in the Canvas tab.

export const Template = (args) => <ToggleSwitch {...args} />

<Canvas>
  <Story name="Controls" args={{
    checked: false, 
    leftLabel: 'off', 
    rightLabel: 'on',
    label: 'ToggleSwitch'
    }}>
    {Template.bind({})}
  </Story>
</Canvas> 