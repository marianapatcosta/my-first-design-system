import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { RadioButton } from "./RadioButton";

<Meta title="Design System/RadioButton" component={RadioButton} />

# RadioButton
<br/><br/> 

### Displays a value of a predefined group of values

Use radio button component when it is intended to choose one value of a predefined group of options, in which each
option correspnds to a radio button.

<Preview withToolbar>
  <Story name="Standard">
      <RadioButton label="option 1"  value= 'option 1'/>
  </Story>
</Preview>

<ArgsTable of={RadioButton} />

### Playground

Experiment with RadioButton Component with the Controls addon in the Canvas tab.

export const Template = (args) => <RadioButton {...args} />

<Canvas>
  <Story name="Controls" args={{
    label: 'option 1',
    value: 'option 1',
  }}>
    {Template.bind({})}
  </Story>
</Canvas> 
