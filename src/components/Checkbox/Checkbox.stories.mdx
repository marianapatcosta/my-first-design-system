import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Checkbox } from "./Checkbox";

<Meta title="Design System/Checkbox" component={Checkbox} />

# Checkbox
<br/><br/> 

### Allows to alternate between two values

Use checkbox when the value alternates between true and false

<Preview withToolbar>
  <Story name="Standard">
     <Checkbox checked={false} label="Checkbox" />
  </Story>
</Preview>

<ArgsTable of={Checkbox} />

### Playground

Experiment with Checkbox Component with the Controls addon in the Canvas tab.

export const Template = (args) => <Checkbox {...args} />

<Canvas>
  <Story name="Controls" args={{
 label: 'Checkbox',
  checked: false,
  }}>
    {Template.bind({})}
  </Story>
</Canvas> 