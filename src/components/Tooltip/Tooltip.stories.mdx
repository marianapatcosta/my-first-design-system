import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Tooltip } from "./Tooltip";

<Meta title="Design System/Tooltip" component={Tooltip} />

# Tooltip
<br/><br/> 

### Displays an helper text

Use Tooltip when some helper text is needed; With extra styling, tooltip can be render on
mouseover or on click of another html element

<Preview withToolbar>
  <Story name="Standard">
    <Tooltip text="Tooltip" style={{ marginLeft: '1.5rem' }} />
  </Story>
</Preview>

<ArgsTable of={Tooltip} />

### Playground

Experiment with Tooltip Component with the Controls addon in the Canvas tab.

export const Template = (args) => <Tooltip {...args} />

<Canvas>
  <Story name="Controls" args={{
  text: 'Tooltip', style: { marginLeft: '1.5rem' }
  }}>
    {Template.bind({})}
  </Story>
</Canvas> 
