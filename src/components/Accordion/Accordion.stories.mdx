import { Meta } from '@storybook/addon-docs/blocks';
import { Accordion } from './Accordion';
import Test from '../../assets/images/test.jpg';

<Meta title='Design System|Accordion' component={Accordion} />

# Accordion

Accordion component

## Usage

Use the Accordion when you need to show a title and the content should only be visible on demand,
by clicking on the component. Both title and content can be string or another components/ HTML elements

- Default Accordion:
  <Story name='Default'>
    <Accordion
      title='Accordion Title'
      content={
        <div>
          Accordion content <img style={{ width: '200px' }} src={Test} />
        </div>
      }
    />
  </Story>

## Other important notes

not applicable.
