import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Card } from './Card';

<Meta title="Design System/Card" component={Card} />

# Card
<br/><br/> 

### Displays any content within a wrapper (card)

Use the card to group element inside a card-like wrapper

<Preview withToolbar>
  <Story name="Standard">
    <Card>
      <div>This is Card component</div>
    </Card>
  </Story>
</Preview>

<ArgsTable of={Card} />

### Playground

Experiment with Card Component with the Controls addon in the Canvas tab.

export const Template = (args) => <Card {...args} />

<Canvas>
  <Story name="Controls" args={{
   children:  <div>This is Card component</div>
  }}>
    {Template.bind({})}
  </Story>
</Canvas> 
