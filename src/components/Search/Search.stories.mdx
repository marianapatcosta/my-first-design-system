import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { Search } from "./Search";

<Meta title="Design System/Search" component={Search} />

# Search
<br/><br/> 

### Displays an input field type `search` to type a keyword to search

Use search field when it is supposed that the user provides some written information for searching purposes

<Preview withToolbar>
  <Story name="Standard">
    <Search value="hello" />
  </Story>
</Preview>

<ArgsTable of={Search} />

### Expansible Search:

  <Story name="Expansible">
    <Search value="hello" isExpandable={true} />
  </Story>

### Search With Label:
  <Story name="WithLabel">
    <Search value="hello" label="Search" />
  </Story>

### Playground

Experiment with Search Component with the Controls addon in the Canvas tab.

export const Template = (args) => <Search {...args} />

<Canvas>
  <Story name="Controls" args={{
   value: 'hello'
  }}>
    {Template.bind({})}
  </Story>
</Canvas> 