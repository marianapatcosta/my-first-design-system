import { Meta } from '@storybook/addon-docs/blocks';
import { Upload } from './Upload';

<Meta title='Design System|Upload' component={Upload} />

# Upload

Uplaod component

## Usage

Use Upload component when it is required to upload a file (audio, image, doc, etc). If `hasPreviews`prop is passed, it displays the
`imagesToPreview` or `filedToPreview`, correpsonding to the uploaded files

- Default Upload:
  <Story name='Default'>
    <Upload label='Upload' />
  </Story>

## Other important notes

not applicable.
