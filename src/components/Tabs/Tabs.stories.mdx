import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs/blocks';
import { styled } from '@storybook/theming';
import { Tabs } from "./Tabs";

export const StyledDiv = styled.div`
  & .fEpyfw {
    position: relative;
  }
`

<Meta title="Design System/Tabs" component={Tabs} />

# Tabs
<br/><br/> 

### Display different content divided among Tabs's Tabs components

Use Tabs when different contents should be shown in separate, according to the user's needs; each 
of these contents is displayed in a Tab component

<Preview withToolbar>
  <Story name="Standard">
    <StyledDiv>
      <Tabs
        tabsMetadata={[
          { label: "Tab 1", content: <div>Tab 1 content</div> },
          { label: "Tab 2", content: <div>Tab 2 content</div> },
        ]}
      />
    </StyledDiv>
  </Story>
</Preview>

<ArgsTable of={Tabs} />

### Playground

Experiment with Tabs Component with the Controls addon in the Canvas tab.

export const Template = (args) => <Tabs {...args} />

<Canvas>
    <Story name="Controls" args={{
    tabsMetadata: [
        { label: 'Tab 1', content: <div>Tab 1 content</div>  },
        { label: 'Tab 2', content: <div>Tab 2 content</div> },
      ]
    }}>
      {Template.bind({})}
    </Story>
</Canvas> 